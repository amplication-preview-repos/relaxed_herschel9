apiVersion: v1
kind: Service
metadata:
  name: {{ include "aplan-insurance-service.fullname" . }}
  labels:
    {{- include "aplan-insurance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aplan-insurance-service.selectorLabels" . | nindent 4 }}
